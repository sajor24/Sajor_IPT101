<UserControl x:Class="SajorIPT101.SajorWPF.Components.EmployeesListingItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SajorIPT101.SajorWPF.Components"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="800">
    <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0 5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding FirstName}" FontWeight="Bold" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Text="{Binding LastName}" FontWeight="Bold" VerticalAlignment="Center" />
            <TextBlock Grid.Column="2" Margin="10 0" VerticalAlignment="Center">
                <Run Text="Age: " />
                <Run Text="{Binding Age}" />
            </TextBlock>
            <TextBlock Grid.Column="3" Text="{Binding Position}" Margin="10 0" VerticalAlignment="Center" />
            <Button Grid.Column="4" Content="Edit" Command="{Binding EditCommand}" CommandParameter="{Binding Employee}" Margin="5 0" Padding="10 5" />
            <Button Grid.Column="5" Content="Delete" Command="{Binding DeleteCommand}" CommandParameter="{Binding Employee}" Margin="5 0" Padding="10 5" Background="#FFFF6B6B" />
        </Grid>
    </Border>
</UserControl>
