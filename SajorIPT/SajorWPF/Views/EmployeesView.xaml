<UserControl x:Class="SajorIPT101.SajorWPF.Views.EmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SajorIPT101.SajorWPF.Views"
             xmlns:components="clr-namespace:SajorIPT101.SajorWPF.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0 0 0 20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Employees" FontSize="32" FontWeight="Bold" />
            <Button Grid.Column="1" Content="Add Employee" Command="{Binding AddEmployeeCommand}" Padding="15 10" Background="#FF4CAF50" Foreground="White" FontWeight="Bold" />
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding EmployeesListingViewModel.EmployeesListingItemViewModels}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <components:EmployeesListingItem />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
